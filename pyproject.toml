[project]
name = "graphiteos"
version = "0.1.0"
description = "Personalized, rooted & modded GrapheneOS OTA patch scripts"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cryptography>=48.0.0",
    "pydantic>=2.13.4",
    "requests>=2.34.2",
    "tomlkit>=0.15.0",
]

[tool.uv]
exclude-newer = "1 week"
